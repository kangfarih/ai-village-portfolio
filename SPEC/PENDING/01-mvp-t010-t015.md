# PENDING 01 — MVP Workstreams T-010..T-015 (AWAITING ORDER)

> Status: **AWAITING ORDER — not cleared for implementation.**
> Source: root `../TASKS.md` Backlog. Titles + short scope + deps below.

## T-010 — Assets: wire soldier/orc sprites + ATTRIBUTION

- Scope: wire player `Soldier_Idle` (48px) + codebot `Orc_Idle` sprites with URL-encoded
  paths and CSS fallback; add `ATTRIBUTION.md` (Zerie credit). Owner: `@coder` WS-A, SPEC: planner W1.
- Deps: Q1 (stack), Q3 (directions), Q5 (assets/credit) from `00-questions.md`.

## T-011 — Animation: delta-time idle/walk

- Scope: rAF loop with `dt` clamp 50ms; idle 6 frames ~8fps / walk 8 frames ~10fps;
  left/right flip; hidden-tab guard. Owner: `@coder` WS-B.
- Deps: T-010; Q3 (direction scheme).

## T-012 — Controls: keyboard + touch buttons

- Scope: WASD/arrows + Enter/E + Esc; 4-dir D-pad + ACTION on coarse pointers;
  guard click-to-move vs buttons. Owner: `@coder` WS-C.
- Deps: T-011 (soft); Q4 (touch scope).

## T-013 — Map: bounds + %-proximity enter/exit

- Scope: clamp movement to 5–95% bounds; world-% proximity (~12%) + `.nearby` class;
  enter/exit guards, no double-count. Owner: `@coder`.
- Deps: T-012; Q2 (building scope).

## T-014 — Content lock: real-data interiors

- Scope: verify About / Skills-16 / Projects-9 / Contact-5 + NPC modals; link fixes only.
  Parallel-safe with T-010..T-013. Owner: `@coder`.
- Deps: none (parallel-safe).

## T-015 — QA smoke gate

- Scope: W6 8-point checklist → `../../TEST-REPORT/` PASS/FAIL per line + SHAs. Owner: `@tester`.
- Deps: T-010..T-014; requires T-002 remote + merge flow to be unblocked.
